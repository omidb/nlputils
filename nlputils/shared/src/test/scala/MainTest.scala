package dgraph

import com.github.omidb.nlp.formats.SExpression
import utest._

import scala.collection.immutable.TreeMap

object DGraphCoreTest extends TestSuite {


  override def tests = TestSuite {
//    'SExpr {
//      val str = "(ROOT\n  (S\n    (NP (PRP He))\n    (VP (VBZ is)\n      (ADJP\n        (NP (CD 5) (NNS feet))\n        (JJ tall)))))"
//      val res = SExpression.parse(str).get
//
//      println(str)
//      println("-------------------------------")
//      println(SExpression.pprint(res))
//
//      //assert(str == SExpression.pprint(res))
//    }
  }


}